import {Transaction} from "./transaction.js";

export const getTransactions = ({
        chainFrom,
        chainTo,
        addressFrom,
        addressTo,
        txnToHash,
        txnFromHash,
        kappa
    }) => {
    // TODO
    return [new Transaction({})]
};